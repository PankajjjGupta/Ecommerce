<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>CYBERSTORE.</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/swiper@11/swiper-bundle.min.css"/>
    <link rel="stylesheet" href="/css/styles.css">
    <style>
        ul li a:hover {
            color: rgba(86, 119, 252, 1);
            transition: .25s ease;
        }
    </style>
</head>
<body class="d-flex flex-column min-vh-100">
    <%- include('../partials/navbar') %>

    <main>
        <%- include('../partials/flash') %>
        <div class="container-fluid" style="min-height: 60rem;"> 
            <div class="row h-100">
                <div class="col-sm-3 h-100 border" style="height : 40rem; min-height : 100%">
                    <div class="user-meta d-flex flex-direction-column align-items-center text-center my-5">
                        <img src="./icons/user.svg" class="img-fluid" alt="" style="width: 5rem;">
                        <h1 class="mt-4" >Admin</h1>
                    </div>
                    <div class="">
                        <ul class="px-0">
                            <li class="my-4 border p-4"><a href="/admin-panel?query=allusers" class="fs-3">All Users</a></li>
                            <li class="my-4 border p-4"><a href="/admin-panel?query=allproducts" class="fs-3">All Products</a></li>
                            <li class="my-4 border p-4"><a href="/admin-panel?query=addproduct" class="fs-3">Add Product</a></li>
                            <li class="my-4 border p-4"><a href="/admin-panel?query=addcarousel" class="fs-3">Add Carousel Products</a></li>
                            <li class="my-4 border p-4"><a href="/admin-panel?query=addhome" class="fs-3">Add Home Products</a></li>
                        </ul>
                    </div>
                </div>
                <div class="col h-100 ">
                    <% if(query == "allproducts") { %>
                        <%- include('../adminpartials/allproducts') %>
                    <% } else if(query == "addproduct") {%>
                        <%- include('../adminpartials/addproduct') %>
                    <% } else if(query == "addcarousel") { %>
                        <%- include('../adminpartials/addcarousel') %>
                    <% } else if(query == "addhome"){ %>
                        <%- include('../adminpartials/addhomeproducts') %>
                    <% } %>
                </div>
            </div>
        </div>
    </main>

    <%- include('../partials/footer') %>
    
    <script src="/js/menu.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/swiper@11/swiper-bundle.min.js"></script>
</body>
</html>