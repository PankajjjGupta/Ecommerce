<style>
    h1 {
        font-size: 4rem;
        font-weight: 400;
    }

    .navigator_header {
        padding: 5rem 0;
        color: #fff;
        position: relative;
        background-color: rgba(86, 119, 252, 1) !important;
    }

    .navigator_links a {
        color: #fff;
        font-size: 1.5rem;
        text-decoration: none;
    }

    .last_link {
        color: red;
    }

    span {
        font-size: 1.5rem;
        padding: 0 .3rem;
        color: #fff;
    }
</style>

<div class="container-fluid bg-info d-flex flex-column navigator_header">
    <h1 class="text-center">Shop</h1>
    <div class="navigator_links m-auto px-4">
        <a href="">Home</a> <span>/</span>
        <a href="">Shop</a> <span>/</span>
        <% if(product) { %>
        <a href=""><%= product.category %></a> <span>/</span>
        <a style="cursor: text; color: #fff;"><%= product.name %></a>
        <% } %>
    </div>
</div>